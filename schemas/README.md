# Schemas

This folder stores the compact Optics schema catalog used by validation. Schema
IDs use the `rusty.optics.*` default namespace.

Regenerate and check the catalog with:

```powershell
cargo run -p rusty-optics-schema -- export --check
```

